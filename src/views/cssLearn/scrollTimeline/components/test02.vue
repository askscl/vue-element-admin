<template>
    <div>
         <div class="tips">
             <!-- 顶部进度条 -->
         </div>
         <div id="g-container">
             <h3>不可思议的纯 CSS 滚动进度条效果 一文中，我们介绍了一种使用渐变实现的纯 CSS 滚动进度指示器效果</h3>
             <p>不可思议的纯 CSS 滚动进度条效果 一文中，我们介绍了一种使用渐变实现的纯 CSS 滚动进度指示器效果，当然，这两处酷炫有意思的转场动画，基于最新的 CSS @scroll-timeline 规范，也是可以大致实现的。本文就将尝试使用纯 CSS，模拟上述的两个转场动画。当然，这两处酷炫有意思的转场动画，基于最新的 CSS @scroll-timeline 规范，也是可以大致实现的。本文就将尝试使用纯 CSS，模拟上述的两个转场动画。当然，这两处酷炫有意思的转场动画，基于最新的 CSS @scroll-timeline 规范，也是可以大致实现的。本文就将尝试使用纯 CSS，模拟上述的两个转场动画。当然，这两处酷炫有意思的转场动画，基于最新的 CSS @scroll-timeline 规范，也是可以大致实现的。本文就将尝试使用纯 CSS，模拟上述的两个转场动画。当然，这两处酷炫有意思的转场动画，基于最新的 CSS @scroll-timeline 规范，也是可以大致实现的。本文就将尝试使用纯 CSS，模拟上述的两个转场动画。当然，这两处酷炫有意思的转场动画，基于最新的 CSS @scroll-timeline 规范，也是可以大致实现的。本文就将尝试使用纯 CSS，模拟上述的两个转场动画。当然，这两处酷炫有意思的转场动画，基于最新的 CSS @scroll-timeline 规范，也是可以大致实现的。本文就将尝试使用纯 CSS，模拟上述的两个转场动画。当然，这两处酷炫有意思的转场动画，基于最新的 CSS @scroll-timeline 规范，也是可以大致实现的。本文就将尝试使用纯 CSS，模拟上述的两个转场动画。当然，这两处酷炫有意思的转场动画，基于最新的 CSS @scroll-timeline 规范，也是可以大致实现的。本文就将尝试使用纯 CSS，模拟上述的两个转场动画。当然，这两处酷炫有意思的转场动画，基于最新的 CSS @scroll-timeline 规范，也是可以大致实现的。本文就将尝试使用纯 CSS，模拟上述的两个转场动画。当然，这两处酷炫有意思的转场动画，基于最新的 CSS @scroll-timeline 规范，也是可以大致实现的。本文就将尝试使用纯 CSS，模拟上述的两个转场动画。当然，这两处酷炫有意思的转场动画，基于最新的 CSS @scroll-timeline 规范，也是可以大致实现的。本文就将尝试使用纯 CSS，模拟上述的两个转场动画。当然，这两处酷炫有意思的转场动画，基于最新的 CSS @scroll-timeline 规范，也是可以大致实现的。本文就将尝试使用纯 CSS，模拟上述的两个转场动画。当然，这两处酷炫有意思的转场动画，基于最新的 CSS @scroll-timeline 规范，也是可以大致实现的。本文就将尝试使用纯 CSS，模拟上述的两个转场动画。当然，这两处酷炫有意思的转场动画，基于最新的 CSS @scroll-timeline 规范，也是可以大致实现的。本文就将尝试使用纯 CSS，模拟上述的两个转场动画。当然，这两处酷炫有意思的转场动画，基于最新的 CSS @scroll-timeline 规范，也是可以大致实现的。本文就将尝试使用纯 CSS，模拟上述的两个转场动画。当然，这两处酷炫有意思的转场动画，基于最新的 CSS @scroll-timeline 规范，也是可以大致实现的。本文就将尝试使用纯 CSS，模拟上述的两个转场动画。当然，这两处酷炫有意思的转场动画，基于最新的 CSS @scroll-timeline 规范，也是可以大致实现的。本文就将尝试使用纯 CSS，模拟上述的两个转场动画。当然，这两处酷炫有意思的转场动画，基于最新的 CSS @scroll-timeline 规范，也是可以大致实现的。本文就将尝试使用纯 CSS，模拟上述的两个转场动画。当然，这两处酷炫有意思的转场动画，基于最新的 CSS @scroll-timeline 规范，也是可以大致实现的。本文就将尝试使用纯 CSS，模拟上述的两个转场动画。当然，这两处酷炫有意思的转场动画，基于最新的 CSS @scroll-timeline 规范，也是可以大致实现的。本文就将尝试使用纯 CSS，模拟上述的两个转场动画。当然，这两处酷炫有意思的转场动画，基于最新的 CSS @scroll-timeline 规范，也是可以大致实现的。本文就将尝试使用纯 CSS，模拟上述的两个转场动画。当然，这两处酷炫有意思的转场动画，基于最新的 CSS @scroll-timeline 规范，也是可以大致实现的。本文就将尝试使用纯 CSS，模拟上述的两个转场动画。当然，这两处酷炫有意思的转场动画，基于最新的 CSS @scroll-timeline 规范，也是可以大致实现的。本文就将尝试使用纯 CSS，模拟上述的两个转场动画。当然，这两处酷炫有意思的转场动画，基于最新的 CSS @scroll-timeline 规范，也是可以大致实现的。本文就将尝试使用纯 CSS，模拟上述的两个转场动画。当然，这两处酷炫有意思的转场动画，基于最新的 CSS @scroll-timeline 规范，也是可以大致实现的。本文就将尝试使用纯 CSS，模拟上述的两个转场动画。当然，这两处酷炫有意思的转场动画，基于最新的 CSS @scroll-timeline 规范，也是可以大致实现的。本文就将尝试使用纯 CSS，模拟上述的两个转场动画。当然，这两处酷炫有意思的转场动画，基于最新的 CSS @scroll-timeline 规范，也是可以大致实现的。本文就将尝试使用纯 CSS，模拟上述的两个转场动画。当然，这两处酷炫有意思的转场动画，基于最新的 CSS @scroll-timeline 规范，也是可以大致实现的。本文就将尝试使用纯 CSS，模拟上述的两个转场动画。当然，这两处酷炫有意思的转场动画，基于最新的 CSS @scroll-timeline 规范，也是可以大致实现的。本文就将尝试使用纯 CSS，模拟上述的两个转场动画。当然，这两处酷炫有意思的转场动画，基于最新的 CSS @scroll-timeline 规范，也是可以大致实现的。本文就将尝试使用纯 CSS，模拟上述的两个转场动画。当然，这两处酷炫有意思的转场动画，基于最新的 CSS @scroll-timeline 规范，也是可以大致实现的。本文就将尝试使用纯 CSS，模拟上述的两个转场动画。当然，这两处酷炫有意思的转场动画，基于最新的 CSS @scroll-timeline 规范，也是可以大致实现的。本文就将尝试使用纯 CSS，模拟上述的两个转场动画。当然，这两处酷炫有意思的转场动画，基于最新的 CSS @scroll-timeline 规范，也是可以大致实现的。本文就将尝试使用纯 CSS，模拟上述的两个转场动画。当然，这两处酷炫有意思的转场动画，基于最新的 CSS @scroll-timeline 规范，也是可以大致实现的。本文就将尝试使用纯 CSS，模拟上述的两个转场动画。当然，这两处酷炫有意思的转场动画，基于最新的 CSS @scroll-timeline 规范，也是可以大致实现的。本文就将尝试使用纯 CSS，模拟上述的两个转场动画。当然，这两处酷炫有意思的转场动画，基于最新的 CSS @scroll-timeline 规范，也是可以大致实现的。本文就将尝试使用纯 CSS，模拟上述的两个转场动画。当然，这两处酷炫有意思的转场动画，基于最新的 CSS @scroll-timeline 规范，也是可以大致实现的。本文就将尝试使用纯 CSS，模拟上述的两个转场动画。当然，这两处酷炫有意思的转场动画，基于最新的 CSS @scroll-timeline 规范，也是可以大致实现的。本文就将尝试使用纯 CSS，模拟上述的两个转场动画。当然，这两处酷炫有意思的转场动画，基于最新的 CSS @scroll-timeline 规范，也是可以大致实现的。本文就将尝试使用纯 CSS，模拟上述的两个转场动画。当然，这两处酷炫有意思的转场动画，基于最新的 CSS @scroll-timeline 规范，也是可以大致实现的。本文就将尝试使用纯 CSS，模拟上述的两个转场动画。当然，这两处酷炫有意思的转场动画，基于最新的 CSS @scroll-timeline 规范，也是可以大致实现的。本文就将尝试使用纯 CSS，模拟上述的两个转场动画。当然，这两处酷炫有意思的转场动画，基于最新的 CSS @scroll-timeline 规范，也是可以大致实现的。本文就将尝试使用纯 CSS，模拟上述的两个转场动画。当然，这两处酷炫有意思的转场动画，基于最新的 CSS @scroll-timeline 规范，也是可以大致实现的。本文就将尝试使用纯 CSS，模拟上述的两个转场动画。当然，这两处酷炫有意思的转场动画，基于最新的 CSS @scroll-timeline 规范，也是可以大致实现的。本文就将尝试使用纯 CSS，模拟上述的两个转场动画。当然，这两处酷炫有意思的转场动画，基于最新的 CSS @scroll-timeline 规范，也是可以大致实现的。本文就将尝试使用纯 CSS，模拟上述的两个转场动画。当然，这两处酷炫有意思的转场动画，基于最新的 CSS @scroll-timeline 规范，也是可以大致实现的。本文就将尝试使用纯 CSS，模拟上述的两个转场动画。当然，这两处酷炫有意思的转场动画，基于最新的 CSS @scroll-timeline 规范，也是可以大致实现的。本文就将尝试使用纯 CSS，模拟上述的两个转场动画。当然，这两处酷炫有意思的转场动画，基于最新的 CSS @scroll-timeline 规范，也是可以大致实现的。本文就将尝试使用纯 CSS，模拟上述的两个转场动画。当然，这两处酷炫有意思的转场动画，基于最新的 CSS @scroll-timeline 规范，也是可以大致实现的。本文就将尝试使用纯 CSS，模拟上述的两个转场动画。当然，这两处酷炫有意思的转场动画，基于最新的 CSS @scroll-timeline 规范，也是可以大致实现的。本文就将尝试使用纯 CSS，模拟上述的两个转场动画。当然，这两处酷炫有意思的转场动画，基于最新的 CSS @scroll-timeline 规范，也是可以大致实现的。本文就将尝试使用纯 CSS，模拟上述的两个转场动画。当然，这两处酷炫有意思的转场动画，基于最新的 CSS @scroll-timeline 规范，也是可以大致实现的。本文就将尝试使用纯 CSS，模拟上述的两个转场动画。当然，这两处酷炫有意思的转场动画，基于最新的 CSS @scroll-timeline 规范，也是可以大致实现的。本文就将尝试使用纯 CSS，模拟上述的两个转场动画。</p>
         </div>
    </div>
</template>
<script>
export default {
    name: 'test02',
    data(){
        return {

        }
    },
    methods:{

    }
}
</script>
<style lang="scss" scoped>
#g-container{ width: 100vw;}
#g-container::before{
    content: "";
    z-index: 10;
    position: fixed;
    height: 5px;
    left: 0;
    top: 0;
    right: 0;
    background: #ffc107;
    animation-name: scale;
    animation-duration: 1s;
    animation-fill-mode: forwards;
    animation-timeline: box-rotate;
    transform-origin:0 50%;
}
@keyframes scale{
    0%{ transform: scaleX(0);}
    100%{ transform: scaleX(1);}
}
@scroll-timeline box-rotate{
    source: auto;
    orientation: vertical;
}
</style>